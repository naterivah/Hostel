<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="form-vertical well fos_user_registration_register">
    <legend>{{'layout.register'|trans([],'FOSUserBundle')}}</legend>    
{{ form_widget(form) }}
    <div>
        <input class="btn btn-inverse" type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>
